#include <iostream>
#include<fstream>
#include<SFML/Audio.hpp>
#include "util/FFT.hpp"
#include "./test/poly.hpp"
#include "test/draw.hpp"

#define count 1024 * 16

int main()
{   
    sf::SoundBuffer buff;
    // C_List v1 = randComplexVector(count);
    // C_List v2 = randComplexVector(count);


    // float t1 = testTime(coefProduct , v1 , v2);
    // float t2 = testTime(valueProduct , v1 , v2);

    // std::cout << "coef product : " << t1 << " s" << std::endl;
    // std::cout << "value product : " << t2 << " s" << std::endl;

    // draw();


    return 0;
}